# Gemini API Configuration
# Required for hybrid and llm_reasoning extraction modes

GEMINI_API_KEY=your-api-key-here

# Model Configuration (Optional)
# Default: gemini-3-pro-preview
# LEGAL_EXTRACTION_MODEL=gemini-3-pro-preview

# Fast model for "fast" quality profile (Optional)
# Default: gemini-3-flash-preview
# LEGAL_EXTRACTION_FAST_MODEL=gemini-3-flash-preview

# Verifier model (Optional)
# Default: same as LEGAL_EXTRACTION_MODEL
# LEGAL_VERIFIER_MODEL=gemini-3-pro-preview

# Rate Limiting Configuration (Optional)
# Number of retries on rate limit errors
# GEMINI_RATE_LIMIT_RETRIES=2

# Base delay in seconds for rate limit backoff
# GEMINI_RATE_LIMIT_BASE_DELAY_SECONDS=1.0
